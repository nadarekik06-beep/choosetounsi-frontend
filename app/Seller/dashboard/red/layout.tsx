'use client';
import RedLayout from '@/app/seller/components/red/layout/RedLayout';

export default function RedDashboardLayout({ children }: { children: React.ReactNode }) {
  return <RedLayout>{children}</RedLayout>;
}